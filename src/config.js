// All editable site text lives in /content.json at the repo root so it can be
// changed without touching code. This file just loads it for the components.
import content from "../content.json";

export default content;
